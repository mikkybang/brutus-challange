{
  "name": "brutus-challange",
  "version": "1.0.0",
  "engines": {
    "node": "10.3.0"
  },
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.0",
    "express-rate-limit": "^4.0.1",
    "mongoose": "^5.7.5",
    "nodemailer": "^6.2.1",
    "nodemailer-sendgrid-transport": "^0.2.0"
  }
}
